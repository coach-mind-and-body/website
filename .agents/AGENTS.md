# Project Rules

## Deployment Rule
- Whenever making code changes to this project, you MUST proactively queue up a `git push` command (`git add . ; git commit -m "..." ; git push`) at the end of the session to push the code live to Railway.
- DO NOT leave code changes sitting in the local environment un-deployed unless explicitly told to do so by the user.
