import { type Express } from "express";
import { type Server } from "http";

export async function setupVite(app: Express, server: Server) {
  // Stubbed out for Next.js migration
}

export function serveStatic(app: Express) {
  // Stubbed out for Next.js migration
}
